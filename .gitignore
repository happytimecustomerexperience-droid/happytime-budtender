.env
.env.dutchie
# GCP / Vertex AI service-account keys — NEVER commit.
data/creds/
*gcp*.json
sa-key.json
stores.json
stores.local.json
*.pyc
__pycache__/
*.sqlite3
.venv/
venv/
.uv/
staticfiles/
.DS_Store
# Local-dev ONLY — publishes :8000 to the host so the local website can reach it.
# Must never reach the VPS (no inbound ports in prod; the Cloudflare tunnel is the
# only ingress). Kept on each dev machine, never committed.
docker-compose.override.yml
test.out
*.out
